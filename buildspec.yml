version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - npm install -g htmlhint

  build:
    commands:
      - echo "Validating HTML..."
      - htmlhint "**/*.html"

artifacts:
  files:
    - '**/*'
